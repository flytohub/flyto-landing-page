# 2026-06-23 Public Site Contract

## Scope

Added a static source contract for the public `flyto2.com` SEO/AEO/GEO surface.
This complements live public-site probes in `flyto-indexer`.

## Changes

- Added `scripts/audit-public-site-contract.mjs`.
- Added `npm run audit:public-site`.
- Updated `npm test` to run both `audit:geo` and `audit:public-site`.
- Added explicit `Claude-SearchBot` and `Applebot` policies to `robots.txt`.
- Linked `robots.txt`, `sitemap.xml`, and `llms.txt` from `public/llms.txt`.

## Verification

```bash
npm run audit:public-site
npm run audit:geo
npm test
```

Result: all passed.

## Live Evidence

The source contract is not a replacement for live reachability. The live evidence
artifact is generated by:

```bash
python /Users/chester/flytohub/flyto-indexer/scripts/write_public_site_verification_evidence.py \
  /Users/chester/flytohub/_audits/flyto2-public-site-2026-06-23 \
  --base-url https://flyto2.com \
  --browser-status ok
```

Current live result: P0 = 0, P1 = 9. Remaining P1s include `/api-docs/` live
404, several AI/search crawler UAs receiving 403, and missing OpenGraph signal.
