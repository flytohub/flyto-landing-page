# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo

# Logs
*.log

# Dependencies
node_modules/

# (Legacy build-time fetched data lived under scripts/cloud-templates.json.)

# Environment / Secrets (never commit)
.env
.env.*
*.env
config.local.json
secrets.json
*.pem
*.key
*.p12

# Build artifacts
dist/
build/
*.min.js.map

# Legacy locale-directory rules removed — old static site no longer exists,
# next-intl emits locale-prefixed routes inside next/out/ which is itself
# already gitignored above.

# Backup files
*.bak
*.backup
*~

# Next.js build artifacts (Workflow rebuilds these on every deploy)
next/.next/
next/out/
next/tsconfig.tsbuildinfo
next/node_modules/

# Allow .env.local.example, block real .env.local
!next/.env.local.example
