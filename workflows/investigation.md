# Investigation workflow

1. Gather the route, locale, browser, and deployment target.
2. Inspect page source and generated metadata, not only the React component.
3. For GEO questions, analyze access logs with `npm run geo:logs`.
4. Separate confirmed behavior from release assumptions.
