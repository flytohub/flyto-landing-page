# Implementation workflow

1. Keep route, metadata, and localized copy changes together.
2. Update `public/llms.txt` or `public/llms-full.txt` when citation paths
   change.
3. Add or update structured data for answer-ready content.
4. Run typecheck and build when dependencies are installed.
5. Record verification in `CHANGELOG.md` or a dated handoff.
