# Wrap-up workflow

1. Run relevant checks and record pass/fail.
2. Update `STATE.md`, `tasks.md`, or a handoff for unresolved work.
3. Confirm `git status --short`.
4. Commit only the intended files.
5. Push and verify local `main` matches `origin/main` when release work is
   complete.
