# Tasks

- [ ] Run `npm run typecheck`.
- [ ] Run `npm run build`.
- [ ] Run `npm run geo:logs -- <access.log> --json <report.json> --csv <report.csv> --markdown <report.md>` on production or staging logs.
- [ ] Verify sitemap and canonical output after any route change.
- [ ] Decide and implement the multilingual hreflang strategy.
- [ ] Add final trust, enterprise, airgap, open-source, compare, and API-docs route inventory.
